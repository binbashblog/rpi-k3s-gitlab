---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: gitlab-pv
  labels:
    name: gitlab-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    server: 1.2.3.4 # NFS server IP
    path: /k3s/gitlab # path on nfs server for container storage
